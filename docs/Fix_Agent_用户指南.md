# Fix Agent 用户指南

## 📖 目录

1. [快速安装](#快速安装)
2. [命令行界面](#命令行界面)
3. [Web界面](#web界面)
4. [核心功能](#核心功能)
5. [高级功能](#高级功能)
6. [使用场景](#使用场景)

---

## 快速安装

### 系统要求

- **Python**: 3.11或更高版本
- **操作系统**:  macOS 10.15+, Ubuntu 18.04+
- **网络**: 稳定的互联网连接

### 安装方式

#### 方式一：pip安装（推荐）

```bash
# 基本安装
pip install Fix_agent

# 完整安装（包含Web界面）
pip install Fix_agent[full]

# 仅Web功能
pip install Fix_agent[web]
```

#### 方式二：源码安装

```bash
# 克隆项目
git clone https://github.com/your-org/fix-agent.git
cd fix-agent

# 安装依赖
pip install -e .
```

### 初始配置

安装完成后，配置AI服务API密钥：

```bash
# 创建配置文件
echo "OPENAI_API_KEY=your-openai-api-key" >> .env

# 或者使用Anthropic Claude
echo "ANTHROPIC_API_KEY=your-claude-api-key" >> .env
```
系统会自动检测是否配置，若未配置，会进入配置脚本引导配置

## 命令行界面

### 启动命令

```bash
# 默认启动
fixagent

# 指定代理名称
fixagent --agent my_agent

# 自动批准模式（工具调用无需确认）
fixagent --auto-approve

# 显示帮助
fixagent help

# 列出所有可用代理
fixagent list
```

### 斜杠命令系统

斜杠命令是Fix Agent CLI的核心功能，提供了完整的会话管理和系统控制。

#### 会话管理命令

```bash
/help          # 显示完整的帮助信息
/clear         # 清屏并重置对话历史
/exit          # 退出Fix Agent CLI
/quit          # 退出Fix Agent CLI（同/exit）
```

#### 目录和文件操作

```bash
/cd <path>     # 切换工作目录
```

**使用示例：**
```bash
> /cd ~/projects                    # 切换到用户主目录下的projects
> /cd ..                            # 切换到上级目录
> /cd /usr/local/bin               # 切换到绝对路径
> /cd ./src/components             # 切换到相对路径
```

#### 配置管理

```bash
/config              # 进入配置管理菜单
```

**配置菜单功能：**
- 创建新的.env配置文件
- 备份现有配置
- 恢复配置备份
- 编辑API密钥和模型设置

#### 记忆系统管理

Fix Agent具备强大的记忆系统，可以学习用户偏好和历史对话。

```bash
/memory              # 显示记忆管理主菜单
/memory help         # 显示记忆管理帮助
/memory edit         # 编辑记忆文件
/memory export       # 导出记忆数据
/memory import       # 导入记忆数据
/memory backup       # 备份当前记忆
/memory restore      # 恢复记忆备份
/memory search       # 搜索记忆内容
/memory stats        # 显示记忆统计信息
/memory files        # 列出所有记忆文件
/memory clean        # 清理过期的记忆文件
```

**记忆系统示例：**
```bash
> /memory stats
📊 记忆统计:
   - 总对话数: 156
   - 工具调用: 1,247
   - 文件引用: 89
   - 存储大小: 12.3 MB
   - 最后更新: 2024-01-15 14:32:10

> /memory search "Python装饰器"
🔍 搜索结果:
   - 找到 12 条相关记录
   - 最近提及: 2024-01-14
```

#### 系统信息

```bash
/sys                 # 显示系统信息
/system              # 显示系统信息（同/sys）
/info                # 显示系统信息（同/sys）
```

**系统信息包含：**
- 操作系统和版本
- Python版本
- Fix Agent版本
- 当前模型配置
- 可用功能特性

#### 资源监控

```bash
/tokens              # 显示当前会话的token使用量
```

**Token监控示例：**
```bash
> /tokens
💰 Token使用统计:
   - 输入Token: 15,234
   - 输出Token: 8,912
   - 总计Token: 24,146
   - 预估费用: $0.72
```

### 模式切换：Human-in-loop vs Auto-approve

#### 模式说明

**Human-in-loop模式（默认）**：
- 每个工具调用都需要用户手动确认
- 更安全，适合生产环境
- 用户可以控制每个操作

**Auto-approve模式**：
- 工具调用自动执行，无需确认
- 更高效，适合可信环境
- 适合批量处理任务

#### 切换方式

**1. 启动时指定**
```bash
fixagent --auto-approve          # 启动时直接进入自动批准模式
```

**2. 运行时切换**
```bash
# 在CLI中按 Ctrl+T 切换模式
# 底部工具栏会实时显示当前模式状态
```

**3. 模式状态显示**

Human-in-loop模式：
```
🔧 模式: manual accept    # 橙色显示
```

Auto-approve模式：
```
🔧 模式: auto-accept ON   # 绿色显示
```

### 快捷键系统

#### 核心快捷键

```bash
Enter                    # 提交消息（如果有自动补全，则应用补全）
Alt+Enter               # 插入换行（多行输入）
ESC+Enter               # 插入换行（Alt+Enter的备选）
Ctrl+E                  # 在外部编辑器中打开当前输入
Ctrl+T                  # 切换auto-approve模式
Ctrl+C                  # 取消当前输入或中断代理工作
Ctrl+J                  # 插入换行（另一个换行快捷键）
```

#### 编辑功能

```bash
↑/↓ 箭头键              # 浏览输入历史
Tab键                   # 触发自动补全
Ctrl+C                  # 强制退出程序
```

### Bash命令集成

Fix Agent支持直接执行bash命令，无需离开CLI界面。

#### 语法格式

```bash
!command                # 执行bash命令
```

#### 使用示例

```bash
> !pwd                   # 显示当前目录
> !ls -la               # 列出详细文件信息
> !python --version     # 检查Python版本
> !git status           # 查看git状态
> !npm install          # 安装npm包
> !docker ps           # 查看运行中的容器
```

#### 平台支持

- **Linux/macOS**: 支持所有标准bash命令
- **Windows**: 支持cmd.exe和PowerShell命令
- **WSL**: 自动检测并支持Windows子系统Linux
- **PowerShell**: 自动检测PowerShell可用性

### 文件引用功能

使用`@`符号可以快速引用文件内容到对话中。

#### 语法格式

```bash
@filename              # 引用文件内容
@path/to/file         # 引用指定路径的文件
@~/config/file        # 使用用户主目录
```

#### 使用示例

```bash
> @main.py            # 引用当前目录的main.py文件
> @src/utils/helper.py # 引用子目录文件
> @~/.bashrc          # 引用用户配置文件
> @config/settings.json # 引用配置文件
```

### 自动补全系统

Fix Agent提供智能自动补全功能，大大提升输入效率。

#### 补全类型

1. **斜杠命令补全**
   - 输入`/`时自动显示可用命令
   - 支持模糊匹配

2. **文件路径补全**
   - 输入`@`时智能匹配文件路径
   - 支持目录递归搜索

3. **Bash命令补全**
   - 输入`!`时显示常用命令
   - 根据平台显示相应命令

#### 使用方法

```bash
# 1. 输入 / 触发斜杠命令补全
> /                   # 按Tab键显示: /help, /clear, /cd, /config, /memory...

# 2. 输入 @ 触发文件补全
> @src/               # 按Tab键显示: main.py, utils/, components/...

# 3. 输入 ! 触发命令补全
> !py                 # 按Tab键显示: python, pytest, pyenv...
```

### 多行输入支持

对于长文本输入，Fix Agent支持多行编辑。

#### 多行输入方法

**1. 使用快捷键**
```bash
Alt+Enter             # 插入换行符
ESC+Enter            # 插入换行符（备选）
Ctrl+J               # 插入换行符（另一个选项）
```

**2. 外部编辑器**
```bash
Ctrl+E               # 在外部编辑器（nano）中编辑
```

#### 多行输入示例

```bash
> 请帮我分析以下Python代码是否存在问题：
   ```python
   def calculate_sum(numbers):
       total = 0
       for num in numbers:
           total += num
       return total
```
   这个函数有什么潜在的改进空间吗？
```

### 实际使用流程示例

#### 完整使用流程

```bash
# 1. 启动Fix Agent
$ fixagent

# 2. 查看帮助信息
> /help

# 3. 切换到项目目录
> /cd ~/my-project

# 4. 检查项目状态
> !git status

# 5. 引用代码文件进行分析
> @src/main.py
> 请分析这段代码的潜在问题

# 6. 切换到自动批准模式进行批量处理
# 按 Ctrl+T

# 7. 查看token使用情况
> /tokens

# 8. 保存对话记忆
> /memory backup

# 9. 退出
> /exit
```

#### 代码分析示例

```bash
# 1. 进入项目目录
> /cd ~/projects/my-app

# 2. 引用需要分析的文件
> @src/utils/database.py

# 3. 请求AI分析
> 请分析这个数据库连接工具类的潜在问题，包括：
>   - 资源泄漏风险
>   - 异常处理
>   - 线程安全性

# 4. 如果需要，引用更多文件
> @config/database.yml

# 5. 查看系统信息确认环境
> /sys
```

---

## Web界面

### 启动Web服务

Fix Agent提供功能完整的Web界面，支持图形化操作。

#### 启动命令

```bash
# 基本启动
fixagent-web

# 开发模式（支持热重载）
fixagent-web-dev

# 指定端口和主机
fixagent-web --host 0.0.0.0 --port 8080

# 后台运行
fixagent-web --daemon

# 停止Web服务
fixagent-web-stop
```

#### 访问地址

启动后，可以通过以下地址访问：

```bash
http://localhost:8000          # 主界面
http://localhost:8000/docs     # API文档
http://localhost:8000/health   # 健康检查
http://localhost:8000/ws/{session_id}  # WebSocket端点
```

### Web界面功能

#### 主要特性

- **实时聊天界面**: 基于WebSocket的双向通信
- **项目管理**: 多项目会话管理
- **记忆同步**: 与CLI共享记忆系统
- **配置管理**: 图形化环境变量配置
- **实时监控**: Token使用和性能监控


#### 核心功能

**1. 实时对话**
- 基于WebSocket的实时通信
- 支持Markdown格式
- 代码高亮显示
- 消息历史记录

**2. 文件管理**
```bash
# 支持的文件操作
- Fix_agent直接通过路径读取文件,无需上传
```

**3. 项目会话**
- 多项目并行管理
- 会话持久化保存
- 快速切换项目
- 会话导出功能

**4. 配置管理**
- 图形化API密钥配置
- 模型选择和参数调整
- 安全级别设置
- 记忆系统配置

### Web界面使用示例

#### 基本使用流程

```bash
# 1. 启动Web服务
$ ./run.py

# 2. 浏览器访问
# 打开 http://localhost:8000

# 3. 配置API密钥（首次使用）
# 点击设置按钮 → API配置 → 输入密钥 → 保存

# 4. 上传项目文件
# 拖拽文件到上传区域 或 点击选择文件

# 5. 开始对话
# 在输入框输入问题，点击发送

# 6. 查看实时响应
# AI响应会实时显示在对话区域
```

---

## 核心功能

### 智能代码分析

Fix Agent具备多维度代码分析能力：

#### 分析类型

```bash
# 语法分析
> 请检查这个Python文件的语法错误和类型问题

# 逻辑分析
> 分析这段代码的逻辑缺陷，包括空指针、资源泄漏等

# 性能分析
> 评估这段代码的性能瓶颈，提出优化建议

# 安全分析
> 检查这段代码是否存在安全漏洞，如SQL注入、XSS等

# 代码质量分析
> 从可读性、可维护性角度评估这段代码
```

#### 使用示例

```bash
# 1. 引用文件
> @src/main.py

# 2. 请求分析
> 请进行全面分析，包括语法、逻辑、性能和安全四个方面

# 3. 获取建议
> 针对发现的问题，请提供具体的修复建议
```

### 自动代码修复

基于AI的智能代码修复功能：

#### 修复模式

```bash
# 交互式修复（推荐）
> 请修复刚才分析出的问题，我会逐一确认

# 自动修复
> 请自动修复所有高优先级问题


### 测试用例生成

自动生成测试代码：

```bash
# 生成单元测试
> 为这个函数生成完整的单元测试，包括边界条件

# 生成集成测试
> 请为这个API端点生成集成测试

# 生成性能测试
> 创建性能测试来验证修复后的性能改进
```

---

## 高级功能

### 多语言支持

Fix Agent支持多种编程语言：

```bash
# Python
> @app.py
> 分析这个Django应用的Python代码

# JavaScript
> @app.js
> 检查这个Node.js应用的JavaScript代码

# Java
> @Main.java
> 分析这个Java Spring应用的代码

# Go
> @main.go
> 检查这个Go服务的代码质量

# C++
> @main.cpp
> 分析这个C++程序的内存管理问题
```

### 批量处理

```bash
# 批量分析目录
> 请分析整个src目录下的所有Python文件

# 批量修复
> 请修复src目录下所有的语法错误和逻辑缺陷

# 分类处理
> 按优先级分类处理问题：先critical，再high，然后medium
```

### 项目理解

```bash
# 项目架构分析
> 请分析这个项目的整体架构和技术栈

# 依赖关系分析
> 绘制这个项目的依赖关系图

# 代码质量评估
> 评估整个项目的代码质量，生成质量报告
```

### 自定义配置

```bash
# 配置修复策略
> 修复时请采用保守策略，最小化代码变更

# 配置分析深度
> 进行深度分析，包括潜在的并发问题

# 配置输出格式
> 请以Markdown格式输出分析结果
```

---

## 使用场景

### 场景1：日常代码审查

```bash
# 1. 进入项目目录
> /cd ~/project

# 2. 分析变更文件
> !git diff --name-only
> @src/changed_file.py
> 请审查这个文件的代码质量

# 3. 生成审查报告
> 请生成详细的代码审查报告，包括改进建议
```

### 场景2：Bug调试

```bash
# 1. 上传错误日志
> @error.log

# 2. 分析相关代码
> @buggy_function.py

# 3. 请求诊断
> 根据错误日志，分析可能的问题原因并提供修复方案
```

### 场景3：代码重构

```bash
# 1. 分析现有代码
> @legacy_code.py
> 分析这个遗留代码的重构价值

# 2. 制定重构计划
> 请制定详细的重构计划，包括步骤和风险评估

# 3. 执行重构
> 按计划逐步重构，保证功能不变
```

### 场景4：性能优化

```bash
# 1. 性能分析
> @slow_function.py
> 分析这个函数的性能瓶颈

# 2. 优化建议
> 提供具体的优化方案和代码实现

# 3. 性能测试
> 生成性能测试来验证优化效果
```

### 场景5：安全审计

```bash
# 1. 安全扫描
> @auth_module.py
> 进行安全漏洞扫描，重点关注权限控制

# 2. 风险评估
> 评估发现的安全风险等级和影响范围

# 3. 安全加固
> 提供安全加固方案和实现代码
```

---

## 快捷参考

### 常用命令速查

```bash
fixagent                    # 启动CLI
fixagent --auto-approve     # 自动批准模式
fixagent-web               # 启动Web界面
fixagent-web-dev           # 开发模式
fixagent-web-stop          # 停止Web服务
```

### 斜杠命令速查

```bash
/help          # 帮助信息
/clear         # 清屏
/cd <path>     # 切换目录
/config        # 配置管理
/memory        # 记忆管理
/sys           # 系统信息
/tokens        # Token使用
/exit          # 退出
```

### 快捷键速查

```bash
Ctrl+T         # 切换auto-approve模式
Ctrl+E         # 外部编辑器
Alt+Enter      # 多行输入
Tab            # 自动补全
Ctrl+C         # 取消/退出
```

### 文件操作速查

```bash
@filename      # 引用文件
!command       # 执行bash命令
/path/to/file  # 文件路径补全
```

---

## 技术支持

- **文档中心**: 访问在线文档获取更多信息
- **问题反馈**: 通过[GitHub Issues](https://github.com/3uyuan1ee/Fix_agent/issues)报告问题
- **邮件支持**: 1481059602@qq.com

---

**Fix Agent v1.0.6**